#pragma once
#include "shapes.h"


void drawCircle(const Circle &c, unsigned color);
void drawAABB(const AABB &B, unsigned color);
void drawPlane(const Plane &B, unsigned color);
void drawHull(const Hull &B, unsigned color);